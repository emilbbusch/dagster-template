# dagster_template

Steps:

(1) Install `uv` (fast package manager for Python)
```shell
pip install uv
```

(2) Create virtual environment
```shell
uv venv
```

(3) Install dependencies
```shell
uv sync
```

(4) Start Dagster
```shell
dagster dev
```